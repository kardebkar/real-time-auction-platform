{
  "name": "@auction/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for auction platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "test": "jest"
  },
  "dependencies": {
    "uuid": "^9.0.0",
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "rimraf": "^5.0.1",
    "@types/uuid": "^9.0.1",
    "jest": "^29.5.0",
    "@types/jest": "^29.5.1"
  }
}
